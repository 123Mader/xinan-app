// 心安 - Android 应用构建配置
plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
    id 'kotlin-kapt'
}

android {
    namespace 'com.xinan.app'
    compileSdk 35

    defaultConfig {
        applicationId "com.xinan.app"
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName "1.0.0"
    }

    buildFeatures {
        viewBinding true
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }
}

dependencies {
    // 核心
    implementation 'androidx.core:core-ktx:1.13.1'
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'com.google.android.material:material:1.12.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    // lifecycle-runtime-ktx (lifecycleScope 扩展, VideoFragment 用)
    implementation 'androidx.lifecycle:lifecycle-runtime-ktx:2.8.7'

    // CameraX (视频模式摄像头)
    implementation 'androidx.camera:camera-core:1.3.4'
    implementation 'androidx.camera:camera-camera2:1.3.4'
    implementation 'androidx.camera:camera-lifecycle:1.3.4'
    implementation 'androidx.camera:camera-view:1.3.4'

    // MediaPipe (人脸关键点 + 本地LLM) - 天玑9000 GPU加速
    implementation 'com.google.mediapipe:tasks-vision:0.10.14'
    implementation 'com.google.mediapipe:tasks-genai:0.10.14'

    // 网络 (后端对接)
    implementation 'com.squareup.okhttp3:okhttp:4.12.0'
    // 或轻量: 用 java.net (ApiClient 已用 HttpURLConnection, 无需 okhttp)

    // 本地数据库 (记忆系统)
    implementation 'androidx.room:room-runtime:2.6.1'
    implementation 'androidx.room:room-ktx:2.6.1'
    kapt 'androidx.room:room-compiler:2.6.1'

    // 协程 (记忆系统/异步)
    implementation 'org.jetbrains.kotlinx:kotlinx-coroutines-android:1.8.1'

    // 情绪分析 (可选: onnxruntime 跑深度学习模型)
    implementation 'com.microsoft.onnxruntime:onnxruntime-android:1.18.0'
}